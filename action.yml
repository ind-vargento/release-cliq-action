name: 'Release Cliq'
description: 'Convert to a Cliq slides template'
inputs:
  message-commit:
    description: 'Message Commit'
    required: true
outputs:
  slides-data: # id of output
    description: 'data parsed'
runs:
  using: 'node16'
  main: 'index.js'